# Trivy scan configuration
# Abort on vulnerabilities with CVSS score >= 7.0
# NVD severity mapping: HIGH = 7.0–8.9 | CRITICAL = 9.0–10.0
severity:
  - HIGH
  - CRITICAL
exit-code: 1
vuln-type:
  - os
  - library
ignore-unfixed: false
